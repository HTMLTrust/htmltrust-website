---
title: 'Search'
description: 'Full-text search across this site. The index is built at deploy time and queried in your browser.'
layout: search
toc: false
# The search page is a tool, not a document: keep it out of page lists and the
# RSS feed while still rendering it.
build:
  list: never
---

This page is the only one on the site that runs JavaScript. The search index is built when the site is deployed and queried locally in your browser, so nothing you type here is sent anywhere.
